# Core runtime dependencies verified in the local `peft` environment.
# Install a PyTorch build that matches your CUDA setup if needed.

numpy==1.24.4
tqdm==4.67.1

torch==2.2.2
transformers==4.46.3
datasets==3.1.0
accelerate==1.1.1
deepspeed==0.16.2
sentencepiece==0.2.0
